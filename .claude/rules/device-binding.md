# 设备绑定规则

- 设备绑定/解绑是核心业务行为，不是普通辅助逻辑。
- 不要随意修改患者、设备、终端之间的绑定关系语义。
- 不要随意修改设备绑定相关 Redis key 前缀、映射方向或数据布局。
- 不要假设不同区域的绑定逻辑是可互换的。
- 修改绑定逻辑时，必须同时考虑绑定、解绑、重复绑定、重绑定和异常恢复场景。
- `DeviceBindingServiceImpl` 属于高风险模块，默认只做局部修改。
- 如果绑定逻辑变化会影响区域切换、状态流转或实时数据展示，必须先说明影响链路。
- 修改后至少要说明如何验证绑定结果和 Redis 状态一致性。
