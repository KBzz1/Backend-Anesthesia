# 接口契约规则

- `接口文档.md` 是前后端契约的最高优先级依据。
- 不要重命名、删除或改变文档中已定义的请求字段、响应字段、字段类型、字段语义。
- 不要默认修改接口路径或接口行为语义。
- 该项目没有稳定的版本化约定，默认保持向后兼容。
- 如果实现与接口文档不一致，先记录差异，再说明影响，不要直接改任一方。
- 如果接口变更确实无法避免，必须先列出受影响接口、字段和前端影响范围。
- 不要把"后端实现更合理"当作修改契约的理由。
