# 验证规则

- 不要声称"测试通过"，除非你实际运行了真实存在的检查。
- 如果没有可靠的自动化检查，必须明确写"需要手工验证"。
- 接口改动后，要列出受影响接口、关键字段和预期行为。
- 状态流转改动后，要列出受影响状态和转换点。
- 设备绑定改动后，要说明如何检查绑定、解绑和 Redis 一致性。
- 实时数据改动后，要说明如何检查接入、持久化、推送链路。
- 如果存在可用脚本，就明确写脚本名；如果没有，不要编造。
- 验证说明要贴合本次改动，不要写模板化空话。
